<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mofe">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:name=".RoomApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".activities.onboarding_activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.add_Item_activity"
            android:screenOrientation="portrait">
        </activity>

        <activity
            android:name=".activities.home_activity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.about_activity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.history_tabs_fragment"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.stats_tabs_fragment"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".activities.add_debtor_activity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>